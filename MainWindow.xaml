<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Code_Editor__AA___WinUI3___Unpakaged_.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Code_Editor__AA___WinUI3___Unpakaged_"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Code_Editor__AA___WinUI3___Unpakaged_"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid x:Name="TitleBarGrid"
          Grid.Row="0"
          Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
          VerticalAlignment="Center">

            <MenuBar VerticalAlignment="Center">
                <MenuBarItem Title="Archivo">
                    <MenuFlyoutItem Text="Nuevo"/>
                    <MenuFlyoutItem Text="Abrir"/>
                </MenuBarItem>

                <MenuBarItem Title="Editar">
                    <MenuFlyoutItem Text="Copiar"/>
                    <MenuFlyoutItem Text="Pegar"/>
                </MenuBarItem>
            </MenuBar>
        </Grid>

        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="8">
            <TextBlock
                Style="{ThemeResource BodyTextBlockStyle}"
                Text="Las veces pulsadas el botón"
                TextAlignment="Center"
                TextWrapping="WrapWholeWords" />
            <TextBlock
                x:Name="NumberText"
                Style="{ThemeResource TitleTextBlockStyle}"
                Text="0"
                TextAlignment="Center"
                TextWrapping="WrapWholeWords" />
            <Button
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Click="Increment_Click"
                Style="{ThemeResource AccentButtonStyle}">
                <StackPanel>
                    <SymbolIcon Symbol="Add" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
